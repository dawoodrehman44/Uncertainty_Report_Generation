name: bayesian_disease_prediction
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scipy
  - scikit-learn
  - pillow
  - pytorch
  - torchvision
  - pip
  - pip:
      # Any pip-only packages you need
